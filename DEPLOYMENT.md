# USTABAŞI - Deployment Guide

## 🚀 Quick Start

```bash
# 1. Install dependencies
npm install

# 2. Set up environment variables
# Create a .env file with your Supabase credentials:
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# 3. Build for production
npm run build

# 4. Deploy to Vercel
vercel --prod
```

## 📦 What's Included

| Directory | Contents |
|-----------|----------|
| `src/` | React frontend source code (pages, components, contexts, lib) |
| `api/` | Vercel serverless API routes (25+ endpoints) |
| `public/` | Static assets (logo, favicon, images) |

## 📊 Tech Stack

- **Frontend:** React 19 + TypeScript + Tailwind CSS v4 + Vite
- **Backend:** Vercel Serverless Functions (Node.js)
- **Database:** Supabase (PostgreSQL)
- **Auth:** Supabase Auth (Email + Google)
- **Maps:** Leaflet + React-Leaflet
- **Animations:** Framer Motion
- **Icons:** Lucide React

## 📝 Database Setup

Run these SQL commands in your Supabase SQL Editor:

```sql
-- Required tables will be auto-created by the app on first run
-- Or manually create: categories, ustas, jobs, offers, messages, reviews,
-- blog_posts, usta_posts, post_likes, post_comments, saved_posts,
-- followers, reports, featured_posts, companies, company_employees,
-- company_jobs, emergency_requests, chat_rooms, chat_messages,
-- tracking, notifications
```

## 🌐 Environment Variables

| Variable | Description |
|----------|-------------|
| `VITE_SUPABASE_URL` | Supabase project URL |
| `VITE_SUPABASE_ANON_KEY` | Supabase anon/public key |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase service role key (server-side only) |
| `VITE_GOOGLE_CLIENT_ID` | Google OAuth Client ID (optional) |
| `VITE_GOOGLE_AUTH_PROXY` | Google Auth Proxy URL (optional) |

## 📚 Project Structure

```
ustabasi/
├── api/                    # Vercel API routes
│   ├── db-client.js          # Supabase client (pre-configured)
│   ├── ustas.js              # Usta CRUD
│   ├── usta_posts.js         # Ustagram posts
│   ├── companies.js          # Company management
│   ├── emergency.js          # Emergency requests
│   ├── tracking.js           # Live tracking
│   └── ...                   # 25+ API endpoints
├── src/
│   ├── pages/                # 20+ page components
│   ├── components/           # Reusable components
│   ├── contexts/             # Auth & Theme contexts
│   ├── lib/                  # Supabase client
│   ├── App.tsx               # Main router
│   └── main.tsx              # Entry point
├── public/                 # Static assets
├── index.html              # HTML entry
├── package.json            # Dependencies
├── vite.config.ts          # Vite config
└── vercel.json             # Vercel deployment config
```

## ⚠️ Excluded Files

The following are NOT included in this package (regenerated on build):

- `node_modules/` - Run `npm install` to restore
- `dist/` - Build output, run `npm run build` to generate
- `.env` - Create your own with your credentials
- `package-lock.json` - Regenerated by npm

## 📞 Contact

- **Email:** ustabasi.official@gmail.com
- **Instagram:** @ustabasi.tr
- **Facebook:** ustabasi.tr
- **Phone:** 0850 123 45 67
